{% extends 'layout.html.twig' %}

{% block title %}{{ brand.user.firstname }}{% endblock %}

{% block body %}
    <h1 class="text-center mt-5">{{ brand.user.firstname }}</h1>

    <div class="container-fluid">
        <div class="row text-center mt-5">
            <div class="offset-1 col-4 mt-5">
                <div class="pictureProfil">
                    <img class="rounded-circle" src="{{ brand.user.picture }}" alt="{{ brand.user.firstname }}">
                </div>
                <br>
                <h3 class="nameProfil">
                    {{ brand.user.firstname }}
                </h3>
                <br>
            </div>
            <div class="descriptionBloc col-4 offset-2 text-justify mx-4 pb-5 px-5 pt-2 align-self-center ">
                <h4 class="text-center">Histoire de la marque</h4>
                <p>{{ brand.description}} </p>
                <ul>
                    {% for category in brand.user.categories %}
                        <li>{{ category.description }}</li>
                    {% else %}
                        <li>Aucun univers n'a été sélectioné</li>
                    {% endfor %}
                </ul>
                <p>Je suis intéressé par le status de : </p>
            </div>
        </div>

        <div class="text-center mt-5">
            <button type="button" class="btn btn-gold">Me contacter</button>
        </div>

        <div class="row text-center mt-5">
            <div class="descriptionBloc offset-1 col-4 text-justify mx-4 pb-5 px-5 pt-2 align-self-center ">
                <h4 class="text-center">Histoire de la marque</h4>
                <p>{{ brand.description}} </p>
                <ul>
                    {% for category in brand.user.categories %}
                        <li>{{ category.description }}</li>
                    {% else %}
                        <li>Aucun univers n'a été sélectioné</li>
                    {% endfor %}
                </ul>
                <p>Je suis intéressé par le status de : </p>
            </div>
            <div class="descriptionBloc col-4 offset-2 text-justify mx-4 pb-5 px-5 pt-2 align-self-center ">
                <h4 class="text-center">Histoire de la marque</h4>
                <p>{{ brand.description}} </p>
                <ul>
                    {% for category in brand.user.categories %}
                        <li>{{ category.description }}</li>
                    {% else %}
                        <li>Aucun univers n'a été sélectioné</li>
                    {% endfor %}
                </ul>
                <p>Je suis intéressé par le status de : </p>
            </div>
        </div>
    </div>
{% endblock %}
