{% extends 'layout.html.twig' %}

{% block title %}Brand{% endblock %}

{% block body %}
    <h1>Brand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brand.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ brand.description }}</td>
            </tr>
            <tr>
                <th>Site</th>
                <td>{{ brand.site }}</td>
            </tr>
            <tr>
                <th>Instagram</th>
                <td>{{ brand.instagram }}</td>
            </tr>
            <tr>
                <th>Host_advantage</th>
                <td>{{ brand.hostAdvantage }}</td>
            </tr>
            <tr>
                <th>Seller_advantage</th>
                <td>{{ brand.sellerAdvantage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('brand_index') }}">back to list</a>

    <a href="{{ path('brand_edit', {'id': brand.id}) }}">edit</a>

    {{ include('brand/_delete_form.html.twig') }}
{% endblock %}
