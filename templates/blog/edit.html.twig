{% extends 'base.html.twig' %}

{% block title %}Modifier un article{% endblock %}

{% block body %}
    <div class="mt-5 container-xl container-lg container-fluid">
        <h1 class="mt-3 mb-4 text-center">Modifier un article</h1>
        <div class="border-gold p-4">
            {{ include('blog/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        </div>

        <div class="row justify-content-between">
            <div class="col col-lg-4">
                <a class="btn btn-dark m-5" href="{{ path('blog_index') }}">
                    <i class="far fa-arrow-alt-circle-left"></i> Liste des articles
                </a>
            </div>
            <div class="col col-lg-3">
                {{ include('blog/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
