{% extends 'layout_admin.html.twig' %}

{% block title %}Modifier les tarifs d'abonnement{% endblock %}

{% block body %}
    <div class="mt-5 container-xl container-lg container-fluid">
        <h1 class="mt-3 mb-4 text-center">Modifier les tarifs d'abonnement</h1>
        <div class="border-gold p-4">
            {{ include('subscription/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        </div>

        <div class="row justify-content-between">
            <div class="col col-lg-4">
                <a class="btn btn-dark m-5" href="{{ path('admin_sub_index') }}"><i class="far fa-arrow-alt-circle-left"></i> Liste des tarifs
                </a>
            </div>
            <div class="col col-lg-3">
                {{ include('subscription/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
