{% extends 'base.html.twig' %}

{% block title %}Article Tag{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center m-5">{{ article_tag.tag }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Description</th>
                <td class="text-center">{{ article_tag.tag }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_article_tag_index') }}" class="btn btn-gold">Retour</a>

    <a href="{{ path('admin_article_tag_edit', {'id': article_tag.id}) }}" class="btn btn-gold">Modifier</a>

    {{ include('article_tag/_delete_form.html.twig') }}
</div>
{% endblock %}
