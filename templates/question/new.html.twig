{% extends 'layout_admin.html.twig' %}

{% block title %}Créer une question{% endblock %}

{% block body %}
    <div class="mt-5 container-xl container-lg container-fluid">
        <h1 class="text-center mt-3 mb-4">Créer une nouvelle question</h1>
        <div class="border-gold p-4">
            {{ include('question/_form.html.twig') }}
        </div>
        <a class="btn btn-dark m-5" href="{{ path('admin_question_index') }}">
            <i class="far fa-arrow-alt-circle-left"></i> Liste des questions
        </a>
    </div>
{% endblock %}
