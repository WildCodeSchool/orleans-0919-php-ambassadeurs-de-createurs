{% extends 'layout_admin.html.twig' %}

{% block title %}Les univers{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center m-5">Les univers</h1>
    <a href="{{ path('admin_category_new') }}" class="btn btn-gold mb-5">Créer un univers</a>
    <table class="table">
        <thead>
            <tr>
                <th>Description</th>
                <th class="col-lg-2">actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.description }}</td>
                <td>
                    <a href="{{ path('admin_category_edit', {'id': category.id}) }}" class="btn btn-gold"><i class="fas fa-pencil-alt mr-2"></i>Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucune catégorie trouvée</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
