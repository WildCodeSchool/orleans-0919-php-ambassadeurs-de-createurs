{% extends '/layout.html.twig' %}

{% block body %}

    <div class="carousel w-100">
        <img class="w-100 filterImage" src="{{ asset('build/champagne-min.jpg') }}" alt="Ambassadeurs de créateurs">
        <div class="carousel-caption">
            <h3 class="d-none d-md-block">REJOIGNEZ NOTRE COMMUNAUTÉ</h3>
            <h1 class="homeTitle">Ambassadeurs de créateurs</h1>
            <h3 class="d-none d-md-block">et développez vos ventes privées à domicile</h3>
        </div>
    </div>

    <div class="greyBack mt-5 mb-5">
        <h2 class="ml-5 p-3">Je recherche : </h2>
        <div class="d-flex justify-content-around">
            <div class="buttonRole">
                <a class="btn btn-gold w-100 mb-4 mb-md-0" href="{{ path('search_role', {'role': 'ambassadeur'}) }}">Ambassadeur</a>
                <h3 class="mt-3 mb-3 w-100 text-center d-none d-md-block">Un ambassadeur pour un créateur</h3>
            </div>
            <div class="buttonRole">
                <a class="btn btn-gold w-100 mb-4 mb-md-0" href="{{ path('search_role', {'role': 'createur'}) }}">Créateur</a>
                <h3 class="mt-3 mb-3 w-100 text-center d-none d-md-block">Une marque, un style</h3>
            </div>
        </div>
    </div>

    <div class="ambassador mt-5 mb-5">
        <h2 class="ml-3 mr-3 mb-5 mt-5 m-md-5 show"> Nos Ambassadeurs</h2>
        <div class="container-fluid card-ambassador">
            <div class="row no-gutters">
                {% for ambassador in ambassadors %}
                <div class="card mb-5 col-md-5 col-sm-12">
                    <div class="row no-gutters">
                        <div class="col-md-1 col-sm-12 mt-2" >
                            <i class="fas fa-star"></i>
                            <p>100</p>
                        </div>
                        <div class="col-md-6 col-sm-12">
                            <div class="card-body">
                                <h5 class="card-title">{{ambassador.firstname}}</h5>
                                <p class="card-text">J'habite dans le {{ambassador.department.codeName}}</p>
                                <p class="card-text"><small class="text-muted">Je suis un {{ambassador.roles}}</small></p>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <img src="{{ ambassador.picture }}" class="card-img" alt="Image">
                        </div>
                    </div>
                </div>
               {% endfor %}
        </div>
    </div>
{% endblock %}
